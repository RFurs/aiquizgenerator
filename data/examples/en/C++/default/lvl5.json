{
  "questions": [
    {
      "name": "C++ - OOP: Lack of Virtual Destructor",
      "questiontext": "Examine the code below. Evaluate it in terms of memory management. Propose a modification and justify it.",
      "questiontype": "essay",
      "code": "#include <iostream>\n\nclass Base {\npublic:\n    Base() { }\n    ~Base() { std::cout << \"Base destroyed\\n\"; }\n};\n\nclass Derived : public Base {\n    int* data;\npublic:\n    Derived() { data = new int[100]; }\n    ~Derived() { \n        delete[] data; \n        std::cout << \"Derived destroyed\\n\"; \n    }\n};\n\nint main() {\n    Base* ptr = new Derived();\n    delete ptr;\n    return 0;\n}"
    },
    {
      "name": "C++ - OOP: Static Member Initialization Safety",
      "questiontext": "Examine the code below. Evaluate the use of the static variable in this class. Propose a modification and justify it.",
      "questiontype": "essay",
      "code": "class Tracker {\npublic:\n    static int count;\n    Tracker() { count++; }\n    ~Tracker() { count--; }\n};\n\nint Tracker::count = 0;\n\nvoid process(Tracker t) {\n    // Perform actions\n}"
    }
  ]
}
